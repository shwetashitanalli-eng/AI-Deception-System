# Dockerfile for Cowrie honeypot (minimal placeholder)
# Replace with the official Cowrie image / build instructions as needed
FROM python:3.11-slim
WORKDIR /cowrie
# Install dependencies (placeholder)
RUN apt-get update && apt-get install -y git libssl-dev libffi-dev build-essential && rm -rf /var/lib/apt/lists/*
COPY . /cowrie
# Install Cowrie and requirements (placeholder)
# RUN pip install -r requirements.txt
CMD ["/bin/bash"]
